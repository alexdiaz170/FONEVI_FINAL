# Sesión — 2026-06-19

## Resumen

### 1. Nombre socio en CreditosLista

- **Backend:** `creditoController.ts` — batch fetch de nombres de socios en handler `list` (igual que AportesLista)
- **Frontend:** `CreditoDTO.nombreSocio`, columna "Socio" en `CreditosLista.tsx`

### 2. Limpieza de datos inconsistentes

#### Scripts ejecutados (ya eliminados):

- `scripts/cleanup-solidaridad.ts` — reemplazó números de documento por nombres de socio en `beneficiario` y `descripcion` (12 registros)
- `scripts/cleanup-inconsistent.ts` — eliminó datos huérfanos

#### Eliminado:

- **5 aportes** con `fechaPago IS NULL` (incompletos)
- **3 aportes** del 22-May sin registro solidaridad correspondiente
- **8 registros solidaridad** (ingresos) sin aporte asociado
- Se conservaron egresos legítimos (Asesoría jurídica $130K, Apoyo educativo $25K)

#### Ahorros recalculados (fórmula: `monto - pagoSolidaridad - pagoCredito`):

| Socio                 | Ahorro     |
| --------------------- | ---------- |
| Alexander Martinez    | $1.330.000 |
| Juan Garcia           | $995.000   |
| Alex Diaz             | $415.000   |
| Carolina Rivera       | $250.000   |
| MARIA DORADO ENRIQUEZ | $0         |
| Demás                 | $0         |

#### Estado final BD: 13 aportes, 11 solidaridad, 8 créditos

### 3. Fixes a lógica de distribución (`DistribucionAporteService`)

#### Issue 1 — Saldo $0 pierde ahorro

- `RegistrarAporteUseCase.ts`: filtro `saldoCapital: { gt: 0 }` en consulta de créditos activos

#### Issue 2 — Interés prorrateado por días

- `CreditoActivo` extends con `fechaDesembolso: Date` y `ultimoPagoFecha: Date | null`
- Factor `díasTranscurridos / 30` (máx 30) aplicado a interés y seguro
- `díasTranscurridos` desde `ultimoPagoFecha ?? fechaDesembolso` hasta `fechaPago`

#### Issue 3 — `abono_credito` salta interés/seguro

- `distribuir()` acepta `tipoOperacion: string` y `fechaPago: Date | null`
- Si `tipoOperacion === 'abono_credito'`: todo va a capital, sin interés ni seguro

#### Archivos modificados:

- `packages/backend/src/domain/services/DistribucionAporteService.ts`
- `packages/backend/src/application/use-cases/aportes/RegistrarAporteUseCase.ts`

### 4. Pendientes para mañana

#### Módulo Reportes:

1. **Botones export faltantes:** Mora, Solidaridad, Dashboard, CreditosPerfil, SociosPerfil
2. **Página `/reportes`:**
   - Balance General
   - Cartera (créditos activos)
   - Estado de Cuenta por socio
   - Flujo de Caja
   - Dividendos (backend existe, frontend no)
3. **Selector de período/mes** para filtrar reportes
4. **Endpoints servidor** tipo `/api/reportes/*` para datos agregados
5. Reemplazar `pages/reportes.html` legacy (roto)

#### Modular Dividendos:

- Frontend para `/api/dividendos` (backend ya existe)

#### Deuda técnica:

- Revisar que el ahorro acumulado se actualice correctamente en SociosPerfil tras crear aporte
- Los solidaridad records del 2026-06-09 para Alex Diaz tienen duplicado (2 registros $5000 para 1 aporte) — no causa daño pero quedó como asimetría
- Considerar agregar `fechaUltimoPago` como columna en `Credito` para evitar consultar `PagoCuota` cada vez
